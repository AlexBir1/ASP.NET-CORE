@model List<WebSchoolMVC.Domain.Entity.Room>

<div id="mainDiv">
    <div class="text-lg-center fs-1" style="margin-bottom:60px">
        Room page <i id="rCreate" class="bi bi-plus-circle link-success" data-url="@Url.Action("Create","Room")" onclick="openPartialClick(this)" style="cursor:pointer"></i>
    </div>
    <br />
    <div class="row border bg-secondary card-list-nobody" >
        @foreach (var x in Model)
        {
            <div class="col-lg-3" style="margin-bottom:15px">
                <div class="card">
                    <div class="card-header">
                        <label class="card-title fs-3">
                            @x.Number
                        </label>
                        <br />
                        <label class="card-subtitle">
                            ROOM ID: @x.Id
                        </label>
                    </div>
                    <div class="card-footer text-center">
                        <a class="btn btn-primary" asp-action="GetById" asp-route-Id="@x.Id">Details</a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

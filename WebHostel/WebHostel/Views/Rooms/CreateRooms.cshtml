@using WebHostel.Domain.Enum
@model WebHostel.Models.RoomsViewModel

<div class="nav-item">
    <div asp-validation-summary="All" class="text-danger"></div>
</div>
<form asp-action="CreateRooms" method="post">
    <div class="card">
        <div class="card-header">
            <h3>Добавить комнату</h3>
        </div>
        <div class="card-body">
            <div class="card-text">
                <label>Номер: </label>
                <input asp-for="num" name="num" placeholder="Номер" />
            </div>
             <div class="card-text">
                <label>Уровень комфорта: </label>
                <select asp-for="rank" name="rank" asp-items="@Html.GetEnumSelectList<RoomRanks>()">
                    <option value="">--Выбрать--</option>
                </select>
            </div>
             <div class="card-text">
                <label>Стоимость за сутки: </label>
                <input asp-for="CostPerDay" name="CostPerDay" placeholder="Стоимость" />
            </div>
             <div class="card-text">
                <label>Максимум человек: </label>
                <input asp-for="maxPeople" name="maxPeople" placeholder="Кол-во" />
            </div>
             <div class="card-text">
                <label>Отель: </label>
                <input asp-for="hostel_name" name="hostel_name" placeholder="Отель" />
            </div>
        </div>
        <div class="card-footer">
            <button class="btn btn-success" type="submit">Добавить</button>
        </div>
    </div>
</form>
